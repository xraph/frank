// Copyright 2023-present XRaph LLC. All rights reserved.
// This source code is licensed under the XRaph LLC license found
// in the LICENSE file in the root directory of this source tree.
// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xraph/frank/ent/organization"
	"github.com/xraph/frank/ent/predicate"
	"github.com/xraph/frank/ent/smstemplate"
	"github.com/rs/xid"
)

// SMSTemplateQuery is the builder for querying SMSTemplate entities.
type SMSTemplateQuery struct {
	config
	ctx              *QueryContext
	order            []smstemplate.OrderOption
	inters           []Interceptor
	predicates       []predicate.SMSTemplate
	withOrganization *OrganizationQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SMSTemplateQuery builder.
func (stq *SMSTemplateQuery) Where(ps ...predicate.SMSTemplate) *SMSTemplateQuery {
	stq.predicates = append(stq.predicates, ps...)
	return stq
}

// Limit the number of records to be returned by this query.
func (stq *SMSTemplateQuery) Limit(limit int) *SMSTemplateQuery {
	stq.ctx.Limit = &limit
	return stq
}

// Offset to start from.
func (stq *SMSTemplateQuery) Offset(offset int) *SMSTemplateQuery {
	stq.ctx.Offset = &offset
	return stq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (stq *SMSTemplateQuery) Unique(unique bool) *SMSTemplateQuery {
	stq.ctx.Unique = &unique
	return stq
}

// Order specifies how the records should be ordered.
func (stq *SMSTemplateQuery) Order(o ...smstemplate.OrderOption) *SMSTemplateQuery {
	stq.order = append(stq.order, o...)
	return stq
}

// QueryOrganization chains the current query on the "organization" edge.
func (stq *SMSTemplateQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(smstemplate.Table, smstemplate.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, smstemplate.OrganizationTable, smstemplate.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SMSTemplate entity from the query.
// Returns a *NotFoundError when no SMSTemplate was found.
func (stq *SMSTemplateQuery) First(ctx context.Context) (*SMSTemplate, error) {
	nodes, err := stq.Limit(1).All(setContextOp(ctx, stq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{smstemplate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (stq *SMSTemplateQuery) FirstX(ctx context.Context) *SMSTemplate {
	node, err := stq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SMSTemplate ID from the query.
// Returns a *NotFoundError when no SMSTemplate ID was found.
func (stq *SMSTemplateQuery) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = stq.Limit(1).IDs(setContextOp(ctx, stq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{smstemplate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (stq *SMSTemplateQuery) FirstIDX(ctx context.Context) xid.ID {
	id, err := stq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SMSTemplate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SMSTemplate entity is found.
// Returns a *NotFoundError when no SMSTemplate entities are found.
func (stq *SMSTemplateQuery) Only(ctx context.Context) (*SMSTemplate, error) {
	nodes, err := stq.Limit(2).All(setContextOp(ctx, stq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{smstemplate.Label}
	default:
		return nil, &NotSingularError{smstemplate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (stq *SMSTemplateQuery) OnlyX(ctx context.Context) *SMSTemplate {
	node, err := stq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SMSTemplate ID in the query.
// Returns a *NotSingularError when more than one SMSTemplate ID is found.
// Returns a *NotFoundError when no entities are found.
func (stq *SMSTemplateQuery) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = stq.Limit(2).IDs(setContextOp(ctx, stq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{smstemplate.Label}
	default:
		err = &NotSingularError{smstemplate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (stq *SMSTemplateQuery) OnlyIDX(ctx context.Context) xid.ID {
	id, err := stq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SMSTemplates.
func (stq *SMSTemplateQuery) All(ctx context.Context) ([]*SMSTemplate, error) {
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryAll)
	if err := stq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SMSTemplate, *SMSTemplateQuery]()
	return withInterceptors[[]*SMSTemplate](ctx, stq, qr, stq.inters)
}

// AllX is like All, but panics if an error occurs.
func (stq *SMSTemplateQuery) AllX(ctx context.Context) []*SMSTemplate {
	nodes, err := stq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SMSTemplate IDs.
func (stq *SMSTemplateQuery) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if stq.ctx.Unique == nil && stq.path != nil {
		stq.Unique(true)
	}
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryIDs)
	if err = stq.Select(smstemplate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (stq *SMSTemplateQuery) IDsX(ctx context.Context) []xid.ID {
	ids, err := stq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (stq *SMSTemplateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryCount)
	if err := stq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, stq, querierCount[*SMSTemplateQuery](), stq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (stq *SMSTemplateQuery) CountX(ctx context.Context) int {
	count, err := stq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (stq *SMSTemplateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryExist)
	switch _, err := stq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (stq *SMSTemplateQuery) ExistX(ctx context.Context) bool {
	exist, err := stq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SMSTemplateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (stq *SMSTemplateQuery) Clone() *SMSTemplateQuery {
	if stq == nil {
		return nil
	}
	return &SMSTemplateQuery{
		config:           stq.config,
		ctx:              stq.ctx.Clone(),
		order:            append([]smstemplate.OrderOption{}, stq.order...),
		inters:           append([]Interceptor{}, stq.inters...),
		predicates:       append([]predicate.SMSTemplate{}, stq.predicates...),
		withOrganization: stq.withOrganization.Clone(),
		// clone intermediate query.
		sql:       stq.sql.Clone(),
		path:      stq.path,
		modifiers: append([]func(*sql.Selector){}, stq.modifiers...),
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *SMSTemplateQuery) WithOrganization(opts ...func(*OrganizationQuery)) *SMSTemplateQuery {
	query := (&OrganizationClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withOrganization = query
	return stq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SMSTemplate.Query().
//		GroupBy(smstemplate.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (stq *SMSTemplateQuery) GroupBy(field string, fields ...string) *SMSTemplateGroupBy {
	stq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SMSTemplateGroupBy{build: stq}
	grbuild.flds = &stq.ctx.Fields
	grbuild.label = smstemplate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SMSTemplate.Query().
//		Select(smstemplate.FieldCreatedAt).
//		Scan(ctx, &v)
func (stq *SMSTemplateQuery) Select(fields ...string) *SMSTemplateSelect {
	stq.ctx.Fields = append(stq.ctx.Fields, fields...)
	sbuild := &SMSTemplateSelect{SMSTemplateQuery: stq}
	sbuild.label = smstemplate.Label
	sbuild.flds, sbuild.scan = &stq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SMSTemplateSelect configured with the given aggregations.
func (stq *SMSTemplateQuery) Aggregate(fns ...AggregateFunc) *SMSTemplateSelect {
	return stq.Select().Aggregate(fns...)
}

func (stq *SMSTemplateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range stq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, stq); err != nil {
				return err
			}
		}
	}
	for _, f := range stq.ctx.Fields {
		if !smstemplate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if stq.path != nil {
		prev, err := stq.path(ctx)
		if err != nil {
			return err
		}
		stq.sql = prev
	}
	return nil
}

func (stq *SMSTemplateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SMSTemplate, error) {
	var (
		nodes       = []*SMSTemplate{}
		_spec       = stq.querySpec()
		loadedTypes = [1]bool{
			stq.withOrganization != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SMSTemplate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SMSTemplate{config: stq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(stq.modifiers) > 0 {
		_spec.Modifiers = stq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, stq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := stq.withOrganization; query != nil {
		if err := stq.loadOrganization(ctx, query, nodes, nil,
			func(n *SMSTemplate, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (stq *SMSTemplateQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*SMSTemplate, init func(*SMSTemplate), assign func(*SMSTemplate, *Organization)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*SMSTemplate)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (stq *SMSTemplateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := stq.querySpec()
	if len(stq.modifiers) > 0 {
		_spec.Modifiers = stq.modifiers
	}
	_spec.Node.Columns = stq.ctx.Fields
	if len(stq.ctx.Fields) > 0 {
		_spec.Unique = stq.ctx.Unique != nil && *stq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, stq.driver, _spec)
}

func (stq *SMSTemplateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(smstemplate.Table, smstemplate.Columns, sqlgraph.NewFieldSpec(smstemplate.FieldID, field.TypeString))
	_spec.From = stq.sql
	if unique := stq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if stq.path != nil {
		_spec.Unique = true
	}
	if fields := stq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, smstemplate.FieldID)
		for i := range fields {
			if fields[i] != smstemplate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if stq.withOrganization != nil {
			_spec.Node.AddColumnOnce(smstemplate.FieldOrganizationID)
		}
	}
	if ps := stq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := stq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := stq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := stq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (stq *SMSTemplateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(stq.driver.Dialect())
	t1 := builder.Table(smstemplate.Table)
	columns := stq.ctx.Fields
	if len(columns) == 0 {
		columns = smstemplate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if stq.sql != nil {
		selector = stq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if stq.ctx.Unique != nil && *stq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range stq.modifiers {
		m(selector)
	}
	for _, p := range stq.predicates {
		p(selector)
	}
	for _, p := range stq.order {
		p(selector)
	}
	if offset := stq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := stq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (stq *SMSTemplateQuery) ForUpdate(opts ...sql.LockOption) *SMSTemplateQuery {
	if stq.driver.Dialect() == dialect.Postgres {
		stq.Unique(false)
	}
	stq.modifiers = append(stq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return stq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (stq *SMSTemplateQuery) ForShare(opts ...sql.LockOption) *SMSTemplateQuery {
	if stq.driver.Dialect() == dialect.Postgres {
		stq.Unique(false)
	}
	stq.modifiers = append(stq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return stq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (stq *SMSTemplateQuery) Modify(modifiers ...func(s *sql.Selector)) *SMSTemplateSelect {
	stq.modifiers = append(stq.modifiers, modifiers...)
	return stq.Select()
}

// SMSTemplateGroupBy is the group-by builder for SMSTemplate entities.
type SMSTemplateGroupBy struct {
	selector
	build *SMSTemplateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (stgb *SMSTemplateGroupBy) Aggregate(fns ...AggregateFunc) *SMSTemplateGroupBy {
	stgb.fns = append(stgb.fns, fns...)
	return stgb
}

// Scan applies the selector query and scans the result into the given value.
func (stgb *SMSTemplateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stgb.build.ctx, ent.OpQueryGroupBy)
	if err := stgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SMSTemplateQuery, *SMSTemplateGroupBy](ctx, stgb.build, stgb, stgb.build.inters, v)
}

func (stgb *SMSTemplateGroupBy) sqlScan(ctx context.Context, root *SMSTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(stgb.fns))
	for _, fn := range stgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*stgb.flds)+len(stgb.fns))
		for _, f := range *stgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*stgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SMSTemplateSelect is the builder for selecting fields of SMSTemplate entities.
type SMSTemplateSelect struct {
	*SMSTemplateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sts *SMSTemplateSelect) Aggregate(fns ...AggregateFunc) *SMSTemplateSelect {
	sts.fns = append(sts.fns, fns...)
	return sts
}

// Scan applies the selector query and scans the result into the given value.
func (sts *SMSTemplateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sts.ctx, ent.OpQuerySelect)
	if err := sts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SMSTemplateQuery, *SMSTemplateSelect](ctx, sts.SMSTemplateQuery, sts, sts.inters, v)
}

func (sts *SMSTemplateSelect) sqlScan(ctx context.Context, root *SMSTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sts.fns))
	for _, fn := range sts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sts *SMSTemplateSelect) Modify(modifiers ...func(s *sql.Selector)) *SMSTemplateSelect {
	sts.modifiers = append(sts.modifiers, modifiers...)
	return sts
}
